name: Temporary export compatibility patch
on:
  push:
    branches:
      - fix/admin-student-stability
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Patch export await
        run: |
          python - <<'PY'
          from pathlib import Path
          p=Path('src/app/pages/admin/AdminOrdersFixed.tsx')
          s=p.read_text()
          old='const result = downloadActiveSalesExcel(orders);'
          new='const result = await downloadActiveSalesExcel(orders);'
          if old not in s:
              raise SystemExit('export call not found')
          s=s.replace(old,new,1)
          p.write_text(s)
          Path('.github/workflows/fix-await-export.yml').unlink(missing_ok=True)
          PY
      - name: Commit patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/app/pages/admin/AdminOrdersFixed.tsx .github/workflows/fix-await-export.yml
          git commit -m "fix: await admin excel export"
          git push
