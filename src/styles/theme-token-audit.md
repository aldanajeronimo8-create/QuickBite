# QuickBite semantic color contract

Legacy student utility classes may remain in JSX for now, but their visual values are routed through semantic QuickBite tokens in `student-theme-token-contract.css`.

Brand controls resolve from `--qb-primary`. Dark mode derives a quieter brand surface from that token. Student surfaces resolve from the existing `--qb-surface` family.
