import { useCallback, useEffect, useMemo, useState, type ComponentType, type ReactNode } from 'react';
import { BarChart3, CalendarDays, FileSpreadsheet, RefreshCw, ShoppingBag, TrendingUp } from 'lucide-react';
import { toast } from 'sonner';
import { requireSupabaseClient, type Order } from '../../../lib/supabase';
import { buildPeriodReportWorkbook } from '../../../services/reportExportService';
import { buildReportPeriod, dateKeyInBogota, formatPeriodDateRange, getMonthWeekGroups, parseReportInputDate, type ReportMode } from '../../../lib/reportPeriods';
import * as XLSX from '@redoper1/xlsx-js-style';
import { Button } from '../../components/ui/button';
import { useAuthStore } from '../../../store/authStore';

const salesHeaders = ['N.º pedido', 'Fecha de compra', 'Hora de compra', 'Cliente', 'Correo', 'Documento', 'Estado pedido', 'Estado pago', 'Método de pago', 'Código recogida', 'Referencia de pago', 'Total pedido', 'Productos', 'Unidades', 'Tiempo estimado (min)'];
const detailHeaders = ['N.º pedido', 'Fecha de compra', 'Hora de compra', 'Producto', 'Categoría', 'Precio unitario', 'Cantidad', 'Subtotal', 'Stock actual', 'Cliente'];
const inputDateFormatter = new Intl.DateTimeFormat('en-CA', { timeZone: 'America/Bogota', year: 'numeric', month: '2-digit', day: '2-digit' });
const dateFormatter = new Intl.DateTimeFormat('es-CO', { timeZone: 'America/Bogota', year: 'numeric', month: '2-digit', day: '2-digit' });
const timeFormatter = new Intl.DateTimeFormat('es-CO', { timeZone: 'America/Bogota', hour: '2-digit', minute: '2-digit', hourCycle: 'h23' });
const currency = new Intl.NumberFormat('es-CO', { style: 'currency', currency: 'COP', maximumFractionDigits: 0 });

function statusLabel(value: string) { return ({ pending: 'Pendiente', preparing: 'En preparación', ready: 'Listo para recoger', delivered: 'Entregado', cancelled: 'Cancelado', confirmed: 'Confirmado', rejected: 'Rechazado' } as Record<string, string>)[value] ?? value; }
function paymentLabel(value: string) { return ({ nequi: 'Nequi', 'bre-b': 'Bre-B', cash: 'Efectivo' } as Record<string, string>)[value] ?? value; }
function toSalesRows(orders: Order[]) { return orders.map((order) => [order.order_number, dateFormatter.format(new Date(order.created_at)), timeFormatter.format(new Date(order.created_at)), order.user?.full_name ?? 'Sin cliente', order.user?.email ?? '', order.user?.ti ?? '', statusLabel(order.status), statusLabel(order.payment_status), paymentLabel(order.payment_method), order.pickup_code ?? '', order.payment_reference ?? '', Number(order.total), Number(order.order_items?.length ?? 0), Number(order.order_items?.reduce((sum, item) => sum + item.quantity, 0) ?? 0), Number(order.estimated_minutes ?? 0)]); }
function toDetailRows(orders: Order[]) { return orders.flatMap((order) => (order.order_items ?? []).map((item) => [order.order_number, dateFormatter.format(new Date(order.created_at)), timeFormatter.format(new Date(order.created_at)), item.product?.name ?? 'Producto no disponible', item.product?.category?.name ?? 'Sin categoría', Number(item.price), Number(item.quantity), Number(item.price) * Number(item.quantity), Number(item.product?.stock ?? 0), order.user?.full_name ?? 'Sin cliente'])); }
function ordersForDay(orders: Order[], day: Date) { return orders.filter((order) => dateKeyInBogota(order.created_at) === day.toISOString().slice(0, 10)); }

export function AdminReports() {
  const authLoading = useAuthStore((state) => state.loading);
  const currentUser = useAuthStore((state) => state.user);
  const [mode, setMode] = useState<ReportMode>('daily');
  const [selectedDate, setSelectedDate] = useState(() => inputDateFormatter.format(new Date()));
  const [orders, setOrders] = useState<Order[]>([]);
  const [loading, setLoading] = useState(true);
  const [exporting, setExporting] = useState(false);
  const selected = useMemo(() => parseReportInputDate(selectedDate), [selectedDate]);
  const period = useMemo(() => buildReportPeriod(mode, selected), [mode, selected]);
  const load = useCallback(async () => {
    if (!currentUser || authLoading) return;
    setLoading(true);
    try {
      const client = requireSupabaseClient();
      const { data, error } = await client.from('orders').select('*, order_items(*, product:products(*, category:categories(*))), user:profiles(*)').gte('created_at', period.startIso).lte('created_at', period.endIso).order('created_at', { ascending: true });
      if (error) throw error;
      setOrders((data ?? []) as unknown as Order[]);
    } catch (error) {
      toast.error(error instanceof Error ? error.message : 'No se pudo cargar el informe.');
      setOrders([]);
    } finally {
      setLoading(false);
    }
  }, [authLoading, currentUser, period]);
  useEffect(() => {
    if (authLoading || !currentUser) return;
    void load();
  }, [authLoading, currentUser?.id, load]);
  const confirmedOrders = useMemo(() => orders.filter((order) => order.payment_status === 'confirmed'), [orders]);
  const totalSales = useMemo(() => confirmedOrders.reduce((sum, order) => sum + Number(order.total), 0), [confirmedOrders]);
  const totalUnits = useMemo(() => confirmedOrders.reduce((sum, order) => sum + Number(order.order_items?.reduce((units, item) => units + Number(item.quantity), 0) ?? 0), 0), [confirmedOrders]);
  const averageTicket = confirmedOrders.length ? totalSales / confirmedOrders.length : 0;
  const dayBreakdown = useMemo(() => period.days.map((day) => { const dayOrders = ordersForDay(orders, day); const confirmed = dayOrders.filter((order) => order.payment_status === 'confirmed'); return { key: day.toISOString().slice(0, 10), label: day.toLocaleDateString('es-CO', { weekday: 'long' }), date: dateFormatter.format(day), active: dayOrders.length > 0, orders: dayOrders.length, sales: confirmed.reduce((sum, order) => sum + Number(order.total), 0), units: confirmed.reduce((sum, order) => sum + Number(order.order_items?.reduce((units, item) => units + Number(item.quantity), 0) ?? 0), 0) }; }), [orders, period.days]);
  const activeDays = dayBreakdown.filter((day) => day.active).length;
  const bestDay = dayBreakdown.reduce((best, day) => day.sales > best.sales || (day.sales === best.sales && day.orders > best.orders) ? day : best, dayBreakdown[0]);
  const worstDay = dayBreakdown.reduce((worst, day) => day.sales < worst.sales || (day.sales === worst.sales && day.orders < worst.orders) ? day : worst, dayBreakdown[0]);
  const monthGroups = useMemo(() => getMonthWeekGroups(period).map((group) => ({ ...group, days: dayBreakdown.filter((day) => group.days.some((date) => date.toISOString().slice(0, 10) === day.key)) })), [dayBreakdown, period]);
  const salesRows = useMemo(() => toSalesRows(orders), [orders]);
  const detailRows = useMemo(() => toDetailRows(orders), [orders]);
  const handleDownload = () => { setExporting(true); try { const workbook = buildPeriodReportWorkbook(orders, period); XLSX.writeFile(workbook, `quickbite-informe-${mode}-${selectedDate}.xlsx`, { compression: true }); toast.success('Informe Excel generado.'); } catch (error) { toast.error(error instanceof Error ? error.message : 'No se pudo generar el Excel.'); } finally { setExporting(false); } };
  const maxSales = Math.max(...dayBreakdown.map((day) => day.sales), 1);
  return <div className="mx-auto max-w-7xl"><div className="mb-7 flex flex-col gap-4 lg:flex-row lg:items-end lg:justify-between"><div><div className="flex items-center gap-3"><BarChart3 className="h-9 w-9 text-blue-700" /><h1 className="text-4xl font-bold text-blue-900">Informes</h1></div><p className="mt-2 text-lg text-slate-600">Web y Excel usan exactamente el mismo período y los mismos datos.</p></div><div className="flex flex-wrap gap-2"><Button variant="outline" onClick={() => void load()} disabled={loading}><RefreshCw className={`mr-2 h-4 w-4 ${loading ? 'animate-spin' : ''}`} />Actualizar</Button><Button onClick={handleDownload} disabled={loading || exporting} className="bg-green-600 text-white hover:bg-green-700"><FileSpreadsheet className="mr-2 h-4 w-4" />{exporting ? 'Generando...' : 'Descargar Excel'}</Button></div></div><section className="mb-6 rounded-3xl border border-slate-200 bg-white p-4 shadow-sm"><div className="flex flex-col gap-4 md:flex-row md:items-center md:justify-between"><div className="flex rounded-2xl bg-slate-100 p-1">{([['daily', 'Diario'], ['weekly', 'Semanal'], ['monthly', 'Mensual']] as const).map(([value, label]) => <button key={value} type="button" onClick={() => setMode(value)} className={`rounded-xl px-5 py-2.5 text-sm font-bold transition ${mode === value ? 'bg-blue-700 text-white shadow' : 'text-slate-600 hover:bg-white'}`}>{label}</button>)}</div><label className="flex items-center gap-3 text-sm font-semibold text-slate-700"><CalendarDays className="h-5 w-5 text-blue-700" /><span>Fecha de referencia</span><input aria-label="Fecha de referencia del informe" type="date" value={selectedDate} onChange={(event) => setSelectedDate(event.target.value)} className="rounded-xl border border-slate-200 px-3 py-2 font-medium outline-none focus:ring-2 focus:ring-blue-200" /></label></div><div className="mt-4 flex flex-wrap gap-2 text-sm"><span className="rounded-full bg-blue-50 px-3 py-1 font-bold text-blue-800">{period.title}</span><span className="rounded-full bg-slate-100 px-3 py-1 text-slate-700">{formatPeriodDateRange(period)}</span><span className="rounded-full bg-slate-100 px-3 py-1 text-slate-700">{period.totalDays} {period.totalDays === 1 ? 'día' : 'días'} totales</span><span className="rounded-full bg-slate-100 px-3 py-1 text-slate-700">{activeDays} {activeDays === 1 ? 'día' : 'días'} con actividad</span></div><p className="mt-3 text-sm text-slate-500">{mode === 'daily' ? 'Solo el día seleccionado.' : mode === 'weekly' ? `Semana completa de lunes a domingo. Semana ${period.weekNumber} de ${period.start.getUTCFullYear()}.` : 'Mes completo con todos sus días agrupados por las semanas correspondientes.'}</p></section><div className="mb-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Kpi icon={TrendingUp} label="Ventas confirmadas" value={currency.format(totalSales)} /><Kpi icon={ShoppingBag} label="Pedidos" value={String(orders.length)} /><Kpi icon={FileSpreadsheet} label="Unidades vendidas" value={String(totalUnits)} /><Kpi icon={BarChart3} label="Ticket promedio" value={currency.format(averageTicket)} /></div><section className="mb-6 rounded-3xl border border-slate-200 bg-white p-5 shadow-sm"><div className="mb-4"><h2 className="text-xl font-bold text-slate-900">{mode === 'daily' ? 'Resumen del día' : mode === 'weekly' ? 'Estructura de la semana' : 'Estructura del mes'}</h2><p className="text-sm text-slate-500">{period.label}</p></div>{mode !== 'monthly' ? <div className="space-y-3">{dayBreakdown.map((day) => <div key={day.key} className="grid grid-cols-[120px_1fr_120px_80px] items-center gap-3 rounded-2xl bg-slate-50 px-3 py-2"><div><p className="text-sm font-bold capitalize text-slate-800">{day.label}</p><p className="text-xs text-slate-500">{day.date}</p></div><div className="h-3 overflow-hidden rounded-full bg-slate-200"><div className="h-full rounded-full bg-blue-600" style={{ width: `${Math.max(day.active ? 3 : 0, (day.sales / maxSales) * 100)}%` }} /></div><div className="text-right text-sm font-bold text-slate-800">{currency.format(day.sales)}</div><div className="text-right text-xs font-semibold text-slate-500">{day.orders} pedidos</div></div>)}</div> : <div className="space-y-5">{monthGroups.map((group) => <div key={group.weekNumber} className="rounded-2xl border border-slate-100 p-4"><div className="mb-3 flex items-center justify-between"><div><p className="font-bold text-slate-900">Semana {group.weekNumber}</p><p className="text-xs text-slate-500">{dateFormatter.format(group.start)} — {dateFormatter.format(group.end)}</p></div><p className="text-xs text-slate-500">{group.days.filter((day) => day.active).length}/{group.days.length} días con actividad</p></div><div className="grid gap-2 md:grid-cols-2 xl:grid-cols-3">{group.days.map((day) => <div key={day.key} className="rounded-xl bg-slate-50 p-3"><div className="flex justify-between gap-3"><span className="text-sm font-semibold capitalize">{day.label}</span><span className="text-xs text-slate-500">{day.date}</span></div><p className="mt-1 font-black text-blue-900">{currency.format(day.sales)}</p><p className="text-xs text-slate-500">{day.orders} pedidos · {day.units} unidades</p></div>)}</div></div>)}</div>}{dayBreakdown.length > 0 && <div className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-3"><Kpi icon={TrendingUp} label="Mejor día" value={`${bestDay.label} · ${currency.format(bestDay.sales)}`} /><Kpi icon={ShoppingBag} label="Día con menor actividad" value={`${worstDay.label} · ${worstDay.orders} pedidos`} /><Kpi icon={CalendarDays} label="Días con actividad" value={`${activeDays}/${period.totalDays}`} /></div>}</section><ReportSection title="Ventas" subtitle="Pedidos pertenecientes únicamente al período seleccionado.">{loading ? <Empty text="Cargando informe..." /> : salesRows.length === 0 ? <Empty text="No hay pedidos en este período." /> : <ReportTable headers={salesHeaders} rows={salesRows} currencyColumns={[11]} />}</ReportSection><ReportSection title="Detalle de productos" subtitle="Detalle de productos pertenecientes únicamente al período seleccionado.">{detailRows.length === 0 ? <Empty text="No hay detalles de productos." /> : <ReportTable headers={detailHeaders} rows={detailRows} currencyColumns={[5, 7]} />}</ReportSection></div>;
}
function Kpi({ icon: Icon, label, value }: { icon: ComponentType<{ className?: string }>; label: string; value: string }) { return <div className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm"><div className="mb-3 flex items-center justify-between"><p className="text-sm font-medium text-slate-500">{label}</p><Icon className="h-6 w-6 text-blue-600" /></div><p className="text-lg font-black text-slate-900">{value}</p></div>; }
function ReportSection({ title, subtitle, children }: { title: string; subtitle: string; children: ReactNode }) { return <section className="mb-6 overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm"><div className="border-b border-slate-100 px-5 py-4"><h2 className="text-xl font-bold text-slate-900">{title}</h2><p className="text-sm text-slate-500">{subtitle}</p></div>{children}</section>; }
function Empty({ text }: { text: string }) { return <div className="p-10 text-center text-slate-500">{text}</div>; }
function ReportTable({ headers, rows, currencyColumns = [] }: { headers: string[]; rows: unknown[][]; currencyColumns?: number[] }) { return <div className="overflow-x-auto"><table className="w-full min-w-[1100px] text-left text-sm"><thead className="bg-green-900 text-xs uppercase text-white"><tr>{headers.map((header) => <th key={header} className="px-4 py-3">{header}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{rows.map((row, rowIndex) => <tr key={`${rowIndex}-${String(row[0])}`} className={rowIndex % 2 === 0 ? 'bg-white' : 'bg-slate-50'}>{row.map((value, columnIndex) => <td key={`${rowIndex}-${columnIndex}`} className="px-4 py-3 align-top">{currencyColumns.includes(columnIndex) && typeof value === 'number' ? currency.format(value) : String(value ?? '')}</td>)}</tr>)}</tbody></table></div>; }
